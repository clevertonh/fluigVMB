function afterProcessCreate(processId){
	/* 
	log.info("###afterProcessCreate");
	 
	 
	 	//Captura o número do processo "Pai"
	    var parentProcess = hAPI.getParentInstance(processId);

	    log.info("PREENCHENDO DADOS DE SUBPROCESSO");
	    log.info(parentProcess);
	    
	    //processo pai diferente de zero -> processo gerado por outro processo
	    if (parentProcess != 0){
	    	hAPI.setCardValue("campo1","aprovado");	
	    }
	    
	    */
	    
	
	
}